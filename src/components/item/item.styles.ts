import { StyleSheet } from "react-native";

export const itemStyles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 7,
  },
  description: {
    flex: 1,
    fontSize: 14,
    fontWeight: 600,
  },
});
