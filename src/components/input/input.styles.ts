import { StyleSheet } from "react-native";

export const inputStyles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    height: 48,
    width: "100%",
    borderRadius: 8,
    paddingHorizontal: 16,
    borderWidth: 1,
    borderColor: "#C3C5CB",
  },
});
